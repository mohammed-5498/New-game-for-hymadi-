# CLAUDE.md — مدينة العصابات (Gang City)

هذا الملف يُقرأ تلقائياً في كل جلسة. اقرأه أولاً ثم اتبعه.

## ما هو المشروع
لعبة استراتيجية في الوقت الحقيقي (RTS) للجوال، بزاوية كاميرا مائلة (isometric) مثل Age of Empires II.
عصابات تتقاتل للسيطرة على أحياء مدينة قديمة شبه مخربة. لا يوجد جمع موارد ولا بناء مبانٍ.

## المراجع الإلزامية (اقرأها قبل كتابة أي كود)
1. `docs/GAME_SPEC.md` — الوصف الكامل والدقيق للعبة. هو المرجع الأعلى.
2. `docs/prototype.html` — نموذج يعمل: توليد الخريطة، رسم المباني، الطقس، التحكم باللمس، التحديد، والحركة حول المباني. انقل منه أسلوب الرسم والتحكم كما هو.
3. `docs/characters.svg` — شكل الشخصيات المميزة الثماني.

## قواعد العمل (مهمة جداً لتوفير الاستهلاك)
- اعمل على **مرحلة واحدة فقط** من قسم "مراحل التنفيذ" في `GAME_SPEC.md`.
- عند انتهاء المرحلة: تأكد أنها تعمل، اعمل commit برسالة واضحة، ثم **توقف** واكتب للمستخدم بالعربية خطوات تجربتها على الجوال.
- لا تبدأ المرحلة التالية إلا عندما يطلب المستخدم ذلك.
- لا تُعِد كتابة ملفات تعمل بدون سبب. عدّل الجزء المطلوب فقط.
- بعد كل مرحلة حدّث قسم "الحالة الحالية" أسفل هذا الملف في سطرين أو ثلاثة.

## قواعد تقنية ثابتة
- HTML5 Canvas 2D + JavaScript عادي بوحدات ES (`type="module"`)، بدون أي أداة بناء وبدون مكتبات ثقيلة.
- يجب أن تعمل اللعبة بفتح `index.html` عبر خادم ملفات بسيط، وعلى GitHub Pages مباشرة.
- كل الأرقام القابلة للموازنة (الدم، الضرر، السرعات، أزمنة الظهور...) في ملف واحد: `js/config.js`. ممنوع كتابتها مباشرة داخل منطق اللعبة.
- الواجهة كلها بالعربية واتجاهها من اليمين لليسار.
- ممنوع إضافة: لعب جماعي عبر الإنترنت، موارد، بناء مبانٍ، أو أي ميزة غير مذكورة في `GAME_SPEC.md` بدون سؤال المستخدم.
- إذا وجدت تعارضاً أو غموضاً في الوصف، اسأل المستخدم سؤالاً واحداً واضحاً بدل التخمين.

## الحالة الحالية
- اكتملت **المرحلة 1**: هيكل الملفات، `config.js`، حلقة اللعبة بخطوة ثابتة (20/ث)، توليد الخريطة بالأحجام الثلاثة، رسم المباني والطقس، الكاميرا واللمس والتحديد، A\* والحركة الجماعية.
- اكتملت **المرحلة 2**: `game/combat.js` — الهجوم التلقائي كل 0.25ث، أمر الهجوم باللمس على عدو، حد المطاردة والعودة، الدم والدرع والموت، أشرطة الدم وومضة الضرب، ومقذوفات الأفاعي مع التحول للقتال القريب.
- اكتملت **المرحلة 3**: `game/capture.js` و`spawn.js` و`victory.js` — الاستيلاء بكل قواعده مع شريط التقدم وتلوين الأرض والإشعار، مؤقتات ظهور الأفراد العاديين والحد الأقصى، والخسارة والفوز مع شاشة نهاية بالإحصائيات.
- اكتملت **المرحلة 4**: `game/abilities.js` — إكسسوارات الأفراد الأربعة، الشخصيات المميزة الثماني بقيمها وقدراتها (ضربة المحطِّم الدائرية، نار رامي النار، هالة الزعيم، علاج الطبيب)، ومكافآت الأحياء المميزة الثلاثة.
- اكتملت **المرحلة 5**: `ai/bot.js` — البوتات بثلاث صعوبات (دفاع ثم توسع ثم هجوم)، التحالفات مع وضع الحلفاء متجاورين على الدائرة، وقاعدة ملاحقة الأقوى عند 40% من الأحياء.
- اكتملت **المرحلة 6**: `ui/menus.js` — القائمة الرئيسية، قائمة الإعداد الكاملة (حجم الخريطة، ثماني خانات لاعبين بعصاباتها وألوانها وفرقها وصعوباتها، الحد الأقصى، الطقس، والتحقق قبل البدء)، قائمة الإيقاف، وشاشة النهاية بزرّيها. `MATCH_DEFAULTS` صار مرجعاً افتراضياً فقط، والأزرار المؤقتة أُزيلت.
- اكتملت **المرحلة 7**: `game/weather.js` — الليل يقصّر مدى الرصد ×0.75 (وحد المطاردة معه)، الثلج يبطئ الحركة ×0.85، والمطر يجعل الرميات تصيب 80% والخاطئة تسقط قرب الهدف بلا ضرر.
- اكتملت **المرحلة 8**: `game/spatialHash.js` (شبكة 2×2)، طابور مسارات بحد 20 عملية A* لكل تحديث، حقل تدفق للمجموعات أكبر من 10، رسم الأرض دفعة واحدة لكل لون، تجاهل الوحدات خارج الشاشة، تبسيط التفاصيل عند الإبعاد، ومؤشر إطارات بثلاث لمسات على شريط المعلومات. `game/loop.js` صار التسلسل الموحد للتحديث.
- **اكتملت كل مراحل `GAME_SPEC.md` الثماني.** أي عمل جديد: أصلح أو حسّن ما هو قائم، أو اسأل المستخدم عما يريد إضافته.
