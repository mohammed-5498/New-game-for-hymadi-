# CLAUDE.md — مدينة العصابات (Gang City)

هذا الملف يُقرأ تلقائياً في كل جلسة. اقرأه أولاً ثم اتبعه.

## ما هو المشروع
لعبة استراتيجية في الوقت الحقيقي (RTS) للجوال، بزاوية كاميرا مائلة (isometric) مثل Age of Empires II.
عصابات تتقاتل للسيطرة على أحياء مدينة قديمة شبه مخربة. لا يوجد جمع موارد ولا بناء مبانٍ.

## المراجع الإلزامية (اقرأها قبل كتابة أي كود)
1. `docs/GAME_SPEC.md` — الوصف الكامل والدقيق للعبة. هو المرجع الأعلى.
2. `docs/prototype.html` — نموذج يعمل: توليد الخريطة، رسم المباني، الطقس، التحكم باللمس، التحديد، والحركة حول المباني. انقل منه أسلوب الرسم والتحكم كما هو.
3. `docs/units-art.js` — رسم كل الوحدات وأنميشنها، جاهز للاستخدام. لا تعد رسم الوحدات من الصفر.
4. `docs/audio.js` — كل مؤثرات الصوت والموسيقى مولّدة بالكود، جاهزة. لا تضف أي ملفات صوتية.

## قواعد العمل (مهمة جداً لتوفير الاستهلاك)
- اعمل على **مرحلة واحدة فقط** من قسم "مراحل التنفيذ" في `GAME_SPEC.md`.
- عند انتهاء المرحلة: تأكد أنها تعمل، اعمل commit برسالة واضحة، ثم **توقف** واكتب للمستخدم بالعربية خطوات تجربتها على الجوال.
- لا تبدأ المرحلة التالية إلا عندما يطلب المستخدم ذلك.
- لا تُعِد كتابة ملفات تعمل بدون سبب. عدّل الجزء المطلوب فقط.
- بعد كل مرحلة حدّث قسم "الحالة الحالية" أسفل هذا الملف في سطرين أو ثلاثة.

## المسار التقني
المشروع يُنقل إلى **Godot 4.x (GDScript)** لبناء تطبيق أندرويد. راجع `GODOT_MIGRATION.md`.
قواعد اللعبة وأرقامها في `docs/GAME_SPEC.md` تبقى كما هي؛ الملغى منها هو القسم 2 وهيكل الملفات والقسم 13.1 فقط.
**الصوت مؤجل:** تجاهل القسم 13.5 و`docs/audio.js` حتى إشعار آخر.

## قواعد تقنية سابقة (نسخة المتصفح — للرجوع فقط)
- HTML5 Canvas 2D + JavaScript عادي بوحدات ES (`type="module"`)، بدون أي أداة بناء وبدون مكتبات ثقيلة.
- يجب أن تعمل اللعبة بفتح `index.html` عبر خادم ملفات بسيط، وعلى GitHub Pages مباشرة.
- كل الأرقام القابلة للموازنة (الدم، الضرر، السرعات، أزمنة الظهور...) في ملف واحد: `js/config.js`. ممنوع كتابتها مباشرة داخل منطق اللعبة.
- الواجهة كلها بالعربية واتجاهها من اليمين لليسار.
- ممنوع إضافة: لعب جماعي عبر الإنترنت، موارد، بناء مبانٍ، أو أي ميزة غير مذكورة في `GAME_SPEC.md` بدون سؤال المستخدم.
- إذا وجدت تعارضاً أو غموضاً في الوصف، اسأل المستخدم سؤالاً واحداً واضحاً بدل التخمين.

## الحالة الحالية
- **المراحل 1 – 9 مكتملة:** الخريطة وبناء APK آلي، والوحدات ورسمها، والقتال،
  والأحياء والظهور والفوز، والعصابات والأبطال والضربات المميزة، والشرطة، والبوتات،
  والقوائم والحفظ.
- **المرحلة 10 (الطقس) مكتملة:** `weather.gd` يرسم الطبقة والإضاءة الليلية الجمعية
  وندف الثلج وخطوط المطر، وأُضيفت زينة الشوارع (براميل وأعمدة إنارة) كمصادر ضوء.
  التأثيرات في `combat.gd`: الرصد ليلاً × 0.75، والسرعة في الثلج × 0.85،
  والهجمات البعيدة في المطر تصيب 80%. 10 مجموعات فحص في `tests/test_weather.gd`.
- **حسمها المستخدم ولا تُغيَّر:** حجم رسم الوحدات `UNIT_SCALE = 0.62` مناسب كما هو،
  ومدة ارتجاجة الإصابة `UNIT_HURT_DUR = 0.35` تبقى كما هي.
- **تعارض حُسم سابقاً:** قوة استيلاء فرد الغربان = **1.25**، و`docs/GAME_SPEC.md` صُحِّح.
- **المرحلة 11 مقسّمة إلى ثلاث دفعات بطلب المستخدم. الدفعة أ مكتملة:** بذرة الشخصية
  (5.4.1) والحركات الأربع ونظام اختيارها بالنقاط (5.4.2) في `combat.gd`، وقوس الضربة
  حسب الحركة في `units_art.gd`. 13 مجموعة فحص في `tests/test_moves.gd`.
- **إصلاح أداء عاجل (14):** اللعبة كانت تتقطّع على الجوال لأن الخريطة كلها كانت
  تُرسم من GDScript في كل إطار. صارت لوحات ثابتة في `draw_layer.gd` لا تُعاد إلا عند
  تغيّر فعلي، مع قصّ ما هو خارج الشاشة، ورسم وحدة أرخص 39%، وحواف ناعمة (msaa_2d)،
  ومؤشر إطارات بثلاث لمسات. `tests/test_perf.gd` يحرس القواعد.
- **إكمال نقل رسم المباني من `prototype.html` (3.6):** النقل الأول كان مبسّطاً
  (صناديق بلا نوافذ، وأنواع غائبة). أُضيفت نوافذ العمارات المضاءة، وأبواب البيوت،
  وركام الأطلال، والمصانع بالدخان، وخزانات الماء، والسيارات المحطمة، وأكشاك السوق،
  والبراميل داخل الأحياء، وعلامات الشوارع. والنسب صارت نسب النموذج نفسها.
- **اللاق على الخريطة الكبيرة (14):** سببه أن لوحة القطر شريط يعبر الخريطة فلا يقصّها
  المحرك. قُطّع كل قطر إلى قطع من 4 مربعات بحدود ضيقة، فنزلت أوامر الرسم من 10392
  إلى 4415 عند التقريب الافتراضي. وأُضيفت الشبكة المكانية (14) وقوة التباعد (4.3).
- **بنود التحكم الناقصة (4.2) أُصلحت كلها:** مربع التحديد المنقّط (لم يكن يُرسم أصلاً)،
  والنقر مرتين لتحديد المجموعة، والرجوع التلقائي الصحيح، والتكبير حول الإصبعين،
  وحدود التكبير 4.5، والضغطة المطوّلة 0.5، وعجلة الماوس، وحدود الكاميرا، والحلقة الحمراء،
  وخط الحلفاء في شريط القوة.
- **الدفعة ب مكتملة (5.4.3):** رؤية الضربة القادمة في قوس 120° ثم الرد بعد زمن رد الفعل،
  والتفادي بقفزة 0.8 مربع بمناعة كاملة، والصدّ لأصحاب الدروع يرتد المهاجم ويترنّحه،
  وقاعدة الزاوية (خلف: لا دفاع و +25% ضرر)، والتحمّل والتبريد، وحالات الرسم الثلاث.
  الصلابة زادت **16%** والوصف يتوقع 15 – 20% فلا حاجة لإعادة موازنة القسم 6.
  14 مجموعة فحص في `tests/test_defense.gd`.
- **الدفعة ج مكتملة (5.4.4 و 5.4.5): وبها اكتملت المرحلة 11 كلها.** الارتداد على الحليف
  يُرنّح الاثنين، والدائرية تدفع الحلفاء بلا ضرر، والجثة تتدحرج بقدر الضربة القاتلة
  وتزحزح من في طريقها (حد 20 جثة)، ومستويات التفصيل الثلاثة (كامل 60 وحدة، مبسّط،
  إحصائي × 0.85) تُحسب كل نصف ثانية. 11 مجموعة فحص في `tests/test_lod.gd`.
- **طابور طلبات المسار (14) مكتمل:** سقف 20 عملية A* في كل تحديث، والباقي ينتظر
  دوره بلا أن يفقد أمره (`path_wait`)، وآخر طلب لكل وحدة يلغي ما قبله.
  6 مجموعات فحص في `tests/test_paths.gd` ومجموعتان في `test_perf.gd`.
  وأُضيف تفصيل رسم حسب حجم الوحدة على الشاشة (بيضاوي بثمانية أضلاع لا بعشرين،
  ولا ظل ولا غبار عند أقصى تصغير).
- **مخزن أشكال الوحدات (14) — البديل الأقوى، بطلب المستخدم:** رسم وحدة واحدة كان
  43 أمر رسم تُعاد كل إطار، وهذا كان البطء الباقي بعد أن صارت الخريطة لوحات ثابتة.
  `unit_mesh.gd` يحوّل شكل الوحدة مرة واحدة إلى مثلثات مفهرسة (كل حالة لها **طور
  واحد** فقط، فُحص ذلك في `_rig`)، ثم تُرسم الوحدة بـ `draw_mesh` واحد.
  **المقيس: 63 → 3.8 ميكروثانية لكل وحدة (17× أقل استهلاكاً للمعالج)، و11 → 1 أمر
  رسم على بطاقة الرسم.** مثلثات لا صوراً: بلا ضبابية عند التقريب، وذاكرة أقل،
  وتُبنى بالحساب فتُفحص بلا شاشة. المطابقة البصرية 0.05% فرق عند تكبير 9× (تنعيم
  حواف فقط). الذاكرة: نحو 2 ميجا لكل (مفتاح، لون)، وسقف 24 ميجا وما بعده يُرسم
  بالطريقة القديمة. الوميض والضربة المميزة لا يُخزَّنان (لونهما يتغير كل إطار).
  11 مجموعة فحص في `tests/test_mesh.gd` ومجموعة تكامل في `test_perf.gd`.
- **حقل التدفق (14): نُفِّذ ثم حُذف بعد القياس — وحُسم بقرار المستخدم.** بُني كاملاً
  ومُختبراً، ثم قِيس على الخريطة الكبيرة 42×42 مقابل A* لكل وحدة:
  20 وحدة 4.3 مقابل **0.7** ms، و60 وحدة 5.3 مقابل **2.2**، و150 وحدة 8.1 مقابل
  **4.8**، و250 وحدة 11.3 مقابل **8.5**. السبب أن `AStarGrid2D` في قودوت أصلي
  بلغة C++، بينما الحقل يُكتب بـ GDScript؛ والوصف كُتب لنسخة المتصفح حيث كان A*
  مكتوباً باليد. وفوق ذلك يُحسب الحقل في إطار واحد بينما الطابور يوزّع A* على
  إطارات. **فالبند 14 «حقل تدفق للمجموعات أكبر من 10» يجعل اللعبة أبطأ في قودوت.**
  و`docs/GAME_SPEC.md` صار يذكر الحذف وسببه وأرقامه صراحة، فلا يُعاد بناؤه ظناً أن فيه نقصاً.
- **قلّل المستخدم مدى الارتداد لكل شيء:** القوية 1 → **0.5** مربع، الصدّ 0.5 → **0.25**،
  الدائرية على الحلفاء 0.5 → **0.25**، صدّة الشرطي 1 → **0.5**، وتدحرج الجثة
  2.5 → **1.2** وزحزحتها 0.35 → **0.18**، ومسافة اصطدام الحليف 0.6 → **0.4**.
  و`docs/GAME_SPEC.md` صُحِّح ليطابقها.
- **اختفاء الوحدة لحظة الضرب — حُلَّ (5.2):** سببه الوميض الأبيض: كان يمزج لون
  الوحدة 55% نحو الأبيض، فعلى أرض الأحياء المميزة وفي الثلج تصير أفتح من الأرض
  فتختفي (ثبت بصورة مقارنة). وكان يُخرجها من مخزن الأشكال فتكلّف 63 ميكروثانية
  بدل 3.8 — وهذا أسوأ ما يكون وقت المعركة. صار **علامة ارتطام** في الطبقة العليا:
  حلقة تتسع، بيضاء بحدّ داكن، تُرى على أي أرض ولا تمس لون الوحدة. والوصف صُحِّح.
- **توزيع العمل على الإطارات (14) — علاج التقطيع عند 250 – 500 وحدة:**
  ما هو خارج الشاشة يُحدَّث مرة كل `STAT_EVERY = 4` إطارات بالزمن المتراكم
  (فُحص: المسافة والضرر **نسبة 1.00** مقابل التحديث كل إطار)، والشبكة المكانية
  تُبنى على مهل في نسخة جانبية ثم تُبدَّل، والتباعد شريحة في كل إطار وربع معدّل
  خارج الشاشة، والهالات تجمع أصحابها أولاً بدل قراءة الإعدادات لكل وحدة.
  **المقيس على 500 وحدة (خريطة كبيرة، اللعبة الحقيقية): متوسط 11.1 → 7.3 ms،
  وأسوأ إطار 19.1 → 9.3.** وفي المقياس المعزول: أسوأ إطار 18.5 → 5.1.
  8 مجموعات فحص في `tests/test_sched.gd` (أُثبتت بأربع طفرات متعمّدة).
- **القراران حُسما بالقياس بتفويض المستخدم:**
  1. **5.4.5:** معامل المستوى الإحصائي **0.85 → 0.74**. الوصف يذكر الرقم وهدفه
     («ألا تختلف النتائج عن القتال المرئي»)، والقياس على ثماني مواجهات قال إن
     0.85 يعطي **+15%**؛ بـ 0.74 صارت النسبة **1.02**. الوصف صُحِّح، و`test_lod.gd`
     صار يفحص **النسبة نفسها** لا الرقم.
  2. **5.4.3/5.4.6:** لا تُعاد موازنة القسم 6. توقّع الوصف «+15 – 20% صلابة» لم
     يتحقق (المقيس 3 – 5%)، لكن **ما أراده الوصف تحقّق في مكانه الصحيح**: قاعدة
     الزاوية تعطي الهجوم من الخلف **+26% سرعة قتل** ومن الجانب **+10%** (150
     مبارزة لكل زاوية). فالالتفاف تكتيك حقيقي، والأرقام تبقى. الوصف صُحِّح بذلك.
- **المرحلة 8 (الأداء والتلميع) — الجولة الأولى:** قِيس شرط القبول (8 لاعبين ×
  250 وحدة، خريطة كبيرة) فظهرت ثلاثة نتوءات: حساب الاستيلاء كان يقارن كل وحدة
  بكل ساحة علم خمس مرات في الثانية (صار فهرس مربعات ثابتاً)، وعدّاد الشرطة كان
  يمسح كل الوحدات **ثماني مرات في كل إطار** (صار مروراً واحداً)، والتباعد كان
  يمسح 6×6 مربعات لجوار نصف مربع (صار يمسح ما تلمسه دائرته فقط، ومربّع المسافة
  بلا جذر). وأصحاب الهالات يُجمعون كل ربع ثانية لا كل إطار.
  **المقيس: 500 وحدة 16.9 → 12.1 ms، و1000 وحدة 37.0 → 28.6 ms.**
  و3 مجموعات فحص جديدة في `test_sched.gd` (صارت 11).
- **(قديم — حُسم أعلاه) سؤال كان مفتوحاً (5.4.5):** الوصف يطلب ضرب ضرر المستوى الإحصائي × 0.85
  «حتى لا تختلف النتائج عن القتال المرئي»، لكن القياس يقول إنه يضرب **+18%**
  لا 0% (سببه أن القتال المرئي يفقد وقتاً في الترنّح والإحصائي لا يترنّح).
  الرقم الذي يحقق هدف الوصف هو نحو **0.72**. لم أغيّره: الوصف يذكر الرقم والهدف
  معاً، وهذا تعارض يحسمه المستخدم.
- **تصحيح قياس سابق:** رقم "الصلابة زادت 16%" كان ضجيج قياس (ثماني تجارب لا تكفي).
  بمئتي تجربة القياس الحقيقي **نحو 3 – 5%** لا 15 – 20% كما يتوقع الوصف 5.4.3.
  الفحص صار يقيس بمئتي تجربة ويطبع الرقم. **موازنة 5.4.3 تحتاج مراجعة بطلب المستخدم.**
- المرحلة التالية: **بانتظار قرار المستخدم** — مراجعة موازنة ردود الأفعال (5.4.3)،
  أو المتابعة إلى ما بعدها.

> حدّث هذا القسم في سطرين أو ثلاثة بعد كل مرحلة.
